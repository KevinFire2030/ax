# 용어 통일본 v2 (특허/PoC 공통)

## 1) 표준 용어(권장)
- **민감정보**: 개인정보 및 기업 비공개 정보 전반을 포괄하는 상위 용어
- **기밀정보**: 민감정보 중 기업 보안등급 상 높은 등급(내부전용/대외비 등)
- **재식별 위험도**: 비식별/가명처리된 정보가 문맥 결합으로 원 식별정보로 추정될 가능성
- **위험도 점수**: 민감 탐지 결과 + 문맥 신호를 정량화한 점수
- **동적 라우팅**: 위험도 및 정책에 따라 내부/외부/혼합 경로를 결정하는 동작
- **혼합 라우팅(Hybrid)**: 세그먼트(문장/라인) 단위로 내부·외부 모델을 병행 적용
- **폴백(Fallback)**: 외부 호출 실패/정책 충돌 시 내부 모델 경로로 자동 전환
- **응답 재검사(Post-check)**: 모델 출력에 대한 2차 DLP 검사
- **감사로그(Audit Log)**: 라우팅 근거/정책 버전/차단 사유를 포함한 추적 로그

## 2) 문서 표기 규칙
1. 제목/요약에서는 "민감정보"를 기본 용어로 사용
2. 기업 보안등급을 강조할 때만 "기밀정보" 병기
   - 예: "민감정보(기밀정보 포함)"
3. "재식별 가능성" 대신 "재식별 위험도"로 통일
4. "자동 분기" 대신 "동적 라우팅"으로 통일
5. "출력 검증" 대신 "응답 재검사(Post-check)"로 통일

## 3) 치환 가이드 (작성/교열 시)
- 기밀 데이터 / 내부 데이터 / 대외비 데이터 → **민감정보** (필요 시 괄호로 기밀정보)
- 재식별 가능성 / 재식별 리스크 / 역식별 위험 → **재식별 위험도**
- 자동 분기 / 스위칭 / 모델 선택 → **동적 라우팅**
- 결과 검수 / 출력 검수 / 응답 검증 → **응답 재검사(Post-check)**

## 4) 권장 문장 템플릿
- "본 발명은 민감정보 위험도에 따라 내부·외부 LLM을 동적으로 라우팅한다."
- "혼합 라우팅 시 세그먼트 단위로 경로를 분리 적용한다."
- "응답 재검사(Post-check) 단계에서 재식별 위험도를 포함한 정책 위반을 판단한다."
