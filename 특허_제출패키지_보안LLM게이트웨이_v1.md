# 변리사 제출 패키지 v1
## 과제명
민감정보 위험도 기반 내부·외부 대규모 언어모델 동적 라우팅 보안 게이트웨이 장치 및 방법

---

## 1) 발명의 명칭
민감정보 위험도 기반 내부·외부 대규모 언어모델 동적 라우팅 보안 게이트웨이 장치 및 방법

---

## 2) 요약(Abstract)
본 발명은 사용자 입력 또는 업무 문서 프롬프트에 대해 DLP(데이터 유출 방지) 분석을 수행하고 민감정보 위험도를 산정하여 내부 LLM 또는 외부 LLM으로 동적으로 라우팅하는 보안 게이트웨이에 관한 것이다. 게이트웨이는 민감 엔티티 탐지, 재식별 위험 점수화, 정책 기반 라우팅, 토큰화/가명화 및 역치환, 응답 재검사 및 차단/경고, 감사 로그 생성을 포함한다. 이에 따라 기밀 유출 리스크를 통제하면서 외부 LLM의 성능을 선택적으로 활용할 수 있다.

---

## 3) 기술적 효과
1. 민감 요청의 외부 전송 억제(보안성 향상)
2. 비민감 요청의 외부 LLM 활용(응답 품질/생산성 향상)
3. 세그먼트 단위 라우팅(품질 저하 최소화)
4. 응답단 재검사(환각/신규 민감정보 노출 억제)
5. 감사로그 기반 컴플라이언스 대응

---

## 4) 도면의 간단한 설명
- 도 1: 시스템 구성도
- 도 2: 입력→탐지→점수화→라우팅→재검사→반환 순서도
- 도 3: 세그먼트 분할 하이브리드 라우팅 흐름도
- 도 4: 토큰화/역치환 및 매핑저장 구조도
- 도 5: 정책 엔진 의사결정 트리
- 도 6: 응답 재검사 및 차단/마스킹 처리도
- 도 7: 감사로그 생성/조회 화면 예시

---

## 5) 청구항(초안 1~10)

### 청구항 1 (독립항: 방법)
입력 데이터를 수신하는 단계;
상기 입력 데이터에 포함된 민감정보를 탐지하고 위험도 점수를 산정하는 단계;
상기 위험도 점수와 정책 규칙에 기초하여 내부 LLM, 외부 LLM 또는 이들의 조합으로 라우팅하는 단계;
LLM 응답을 수신한 후 민감정보 포함 여부를 재검사하는 단계; 및
재검사 결과에 따라 응답 제공, 마스킹 또는 차단을 수행하는 단계를 포함하는,
민감정보 위험도 기반 LLM 라우팅 방법.

### 청구항 2
청구항 1에 있어서, 상기 위험도 점수 산정은 규칙 기반 탐지 결과, 엔티티 인식 결과 및 문맥 기반 재식별 위험도를 조합하여 수행되는 방법.

### 청구항 3
청구항 1에 있어서, 상기 라우팅 단계는 입력 데이터를 복수 세그먼트로 분할하고 세그먼트별 위험도에 따라 내부 LLM 또는 외부 LLM으로 개별 라우팅하는 방법.

### 청구항 4
청구항 1에 있어서, 외부 LLM으로 전송되는 데이터는 토큰화 또는 가명화 처리된 데이터인 방법.

### 청구항 5
청구항 4에 있어서, 토큰-원문 매핑 정보는 암호화 저장소 또는 접근통제 저장소에 보관되는 방법.

### 청구항 6
청구항 1에 있어서, 상기 재검사 단계에서 신규 민감정보 또는 정책 위반 패턴이 검출되는 경우 응답을 차단하거나 마스킹하는 방법.

### 청구항 7
청구항 1에 있어서, 정책 규칙은 사용자 역할, 데이터 등급, 업무 유형, 시간대, 모델 신뢰도 중 적어도 하나를 기준으로 동적으로 적용되는 방법.

### 청구항 8
청구항 1에 있어서, 위험도 점수, 적용 정책 버전, 라우팅 대상 모델 식별자 및 차단 사유를 포함하는 감사 로그를 생성하는 방법.

### 청구항 9
청구항 1에 있어서, 외부 LLM 호출 실패 또는 정책 충돌 발생 시 내부 LLM으로 자동 폴백하는 방법.

### 청구항 10 (독립항: 장치)
민감정보 탐지 모듈, 위험도 산정 모듈, 정책 엔진, 라우팅 모듈, 토큰화/역치환 모듈, 응답 재검사 모듈 및 감사 로그 모듈을 포함하고, 청구항 1 내지 9 중 어느 한 항의 방법을 수행하는 LLM 보안 게이트웨이 장치.

---

## 6) 신규성/진보성 주장 포인트(핵심 10)
1. 위험도 정량화 기반 라우팅
2. 문맥 기반 재식별 위험 평가
3. 세그먼트 단위 하이브리드 라우팅
4. 전송 전 토큰화 + 제한적 역치환
5. 응답단 2차 DLP 재검사
6. 동적 정책 엔진 적용
7. 라우팅 사유 감사로그 구조화
8. 외부 장애/충돌 시 보안 우선 폴백
9. 성능-보안 동시 최적화
10. 기존 시스템 비침습 도입 가능성

---

## 7) 거절 대응 예상 Q&A (5)

### Q1. DLP+라우팅 단순 결합 아닌가?
A1. 재식별 위험 점수화, 세그먼트 병렬 라우팅, 응답 재검사 및 제한적 역치환의 결합으로 기능적 상호작용이 존재함.

### Q2. 민감/비민감 분기는 자명하지 않은가?
A2. 이분 분기가 아니라 연속 점수 기반 동적 정책 적용이며, 세그먼트 단위 모델 선택까지 포함됨.

### Q3. 토큰화는 공지기술 아닌가?
A3. 본 발명은 토큰화를 라우팅/재검사/역치환 통제와 결합한 운영 제어 구조임.

### Q4. 출력 재검사는 일반 보안 절차 아닌가?
A4. 입력 위험도·정책·라우팅 이력과 연동한 폐루프 검증 체계로 차별됨.

### Q5. 산업상 이용 가능성이 약한가?
A5. 게이트웨이 전단 삽입 구조로 실제 기업/공공 환경 적용이 용이하고, 감사/컴플라이언스 대응성이 높음.

---

## 8) 변리사 검토 요청 포인트
1. 독립항 범위(방법/장치/기록매체) 확장 여부
2. 세그먼트 라우팅을 핵심 청구항으로 올릴지 여부
3. 역치환 제한 조건(허용 토큰 집합) 문언 구체화
4. 정책 엔진 요소(권한/등급/시간/모델신뢰도) 필수/선택 구분
5. 해외(PCT) 확장 대비 용어 정합화

---

## 9) 제출 전 체크리스트
- [ ] 구현 흐름도(도 1~7) 작성
- [ ] 실시예(정상/차단/폴백/혼합 라우팅) 로그 캡처 확보
- [ ] 용어 통일(민감정보/기밀정보/재식별 위험도)
- [ ] 선행기술 대비표(차별점 매트릭스) 작성
- [ ] 출원 전략(국내 우선→PCT) 일정 확정
